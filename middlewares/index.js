const validateBody = require("./validateBody");
const auth = require("./auth");
const upload = require("./upload");
const passport = require("./passport");

module.exports = { validateBody, auth, upload, passport };
